1. フォルダ構成

手動DLフォルダはどこか
コード内に変数とメモで書いてて。あとでその変数を実際のファイルパスで書き直す。メモもつけてて
.mp4 だけを移す一時処理フォルダはどこか
コード内に変数とメモで書いてて。あとでその変数を実際のファイルパスで書き直す。メモもつけてて
最終保存先のベースフォルダはどこか
コード内に変数とメモで書いてて。あとでその変数を実際のファイルパスで書き直す。メモもつけてて
未照合、保留、エラー用フォルダを作るか
つくる、作る先は
コード内に変数とメモで書いてて。あとでその変数を実際のファイルパスで書き直す。メモもつけてて

CSVログの保存先フォルダはどこか
コード内に変数とメモで書いてて。あとでその変数を実際のファイルパスで書き直す。メモもつけてて

2. 監視と移動

DLフォルダを常時監視するか、定期実行で確認するか
DLフォルダ常時監視だとうれしいが、業務負荷により、定時にするかもしれない。いまは保留。優先は常に
ダウンロード中ファイルをどう判定するか
完了するまで待つ
正確には、まずmp4ファイルだけ、移動させるプロンプトを常時監視で動かしてＤＢとリネームはあとってmdに書いた気がするけど
.mp4 以外は無視でよいか
無視でよい
同名ファイルが一時フォルダにある場合どうするか
（１）として、かぶりがわかるように保存
移動失敗時に元ファイルをどう扱うか
そのまま触らずに、もとのフォルダに残す
3. 商品番号抽出

ファイル名から商品番号を抽出するルール
7桁固定でよいか
6桁もある。
FC2-PPV-1234567, FC2PPV-1234567, fc2ppv-1234567 などを対象にするか
まずこの形でくるが固定値でないので、ファイル名を数値だけのファイル名にしようと考える。その場合のパターンも決めておく
ここはこの処理の時に固めるのでこの処理を書くときにきいておくれ
_1, -2, -01 などの枝番をどう扱うか
枝番は元ファイルに地底たら元ファイルの形を崩さずに尊重して残す

番号が複数見つかった場合どうするか
（１）（２）などつけてわかるようにしてて
番号が見つからない場合どうするか
何と比較して？ＤＢのマスター？
これは見つからなかったフォルダを用意して、そこに元ファイル名のまま、リネームなしで移動だけする
4. DB照合

照合先は xxx_VQ001_moviemaster_unique 想定でよいか
viewでよい
商品番号一致だけで自動確定してよいか
よい。1意がそれしかないだろう
タイトル類似照合は初期段階では使わないか
いらない
ＩＤのみ
第３世紀の形をくずすな
照合できないファイルはどのテーブル、どのフォルダへ送るか
先ほど書いたが、できなかったフォルダをよういして移動させる、ろぐにはそのように記載
Moved 旧ログの救済も同じ照合処理に乗せるか
載せてよい。手動で走らせるプロンプトをさきに作る。じどうは後から。全部自動化するのをさいしょからやらない。小さく作っていく

5. リネーム

新ファイル名の基本形
例: FC2 PPV {product_id} {title}.mp4
Windows禁止文字の置換ルール
長すぎるファイル名の短縮ルール
同名ファイルがある場合の枝番ルール
もともとの枝番 _1, -2 を維持するか
(1) のような重複候補は新規処理で作るか、避けるか

ここの質問、重複ばかりだぞ？上に書いている、わからなければ再度しつもんしてくれ

6. カテゴリ移動

最終フォルダは seller 単位でよいか
よい。セラー単位で振り分け、その名前のフォルダがなければ新しいフォルダをセラーの名前で作成して格納
seller_id を使うか、seller group / canonical seller name を使うか
物理名でフォルダ名は作成してくれ、でないとみてわからない

seller 名が未確定の場合はどこへ置くか
未名称というフォルダを作ってそこに入れて。まえのjsでもにたことしてたよね？読んだよね？踏襲してね
フォルダ名の禁止文字置換ルール
禁止文字は入れない。飛ばして
ドライブ振り分け、pathid の決定ルール
これは決定したPathを入れればいい。ストレージの問題は、手動で管理監視して書き換えるから。そのために保存先の変数を、わっかりやすく、メモつけて書いてとさきに言った

既存の保存場所と新規保存場所の関係
ファイル数と、ストレージの負荷から既存の場所（DB記載あるもの）は移動しない、書き換えない
7. DB登録

登録先は xxx_TM002_owned_files でよいか
よい

未照合ファイルは別テーブルにする方針でよいか
よい

登録タイミングは「移動成功後」でよいか
OK
file_size, file_modified_at をいつ取得するか
完全移動後

source, matched_by, status の値ルール
DB登録失敗時にファイルはどう扱うか
ログでエラー残して。
後で手動で入力できるSQLも用意してて
8. ログとCSV

DBログとCSVログは同じ内容でよいか
OK
run_id の命名ルール
どの処理単位で1行ログを残すか
データ1個ずつ
dry-run 時もCSVを出すか
これなんだけ？
DB書き込み失敗時もCSVだけは必ず残すか
残す
9. Dry-run

dry-run で実際にファイルを動かさない
予定リネーム名、予定移動先、DB登録予定を出す
dry-run 結果をCSVに出す
dry-run の確認後に本実行する流れにする
10. 最初の小さな実装単位

まず testcsv の安全対象を読むだけにするか
それとも実フォルダ内の数件ファイルを対象にするか
初回は dry-run のみでよいか
本実行対象は何件から始めるか
この中で最初に決めるべきは 1. フォルダ構成 です。
ここが決まると、監視、ログ、保留、移動、DB登録の流れが一気に具体化します。

9番がまだ理解できてない、説明を求む